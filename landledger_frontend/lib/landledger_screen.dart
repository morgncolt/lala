import 'package:flutter/material.dart';

class LandledgerScreen extends StatelessWidget {
  const LandledgerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LandLedger Analytics')),
      body: const Center(child: Text('LandLedger coming soon!')),
    );
  }
}
