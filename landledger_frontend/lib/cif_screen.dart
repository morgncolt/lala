import 'package:flutter/material.dart';

class CifScreen extends StatelessWidget {
  const CifScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cif Analytics')),
      body: const Center(child: Text('Cif coming soon!')),
    );
  }
}
